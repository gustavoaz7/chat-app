{
  "name": "@team-chat/database",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "prisma": "^6.9.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "prisma generate --schema prisma/schema.prisma",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src/database.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
