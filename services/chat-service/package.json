{
  "name": "@services/chat-service",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "dependencies": {
    "@team-chat/contracts": "workspace:*",
    "@prisma/client": "^6.7.0",
    "fastify": "^4.29.1"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  },
  "scripts": {
    "build": "node -e \"console.error('build is not implemented yet for @services/chat-service'); process.exit(1)\"",
    "dev": "node -e \"console.error('dev is not implemented yet for @services/chat-service'); process.exit(1)\"",
    "lint": "node -e \"console.error('lint is not implemented yet for @services/chat-service'); process.exit(1)\"",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
